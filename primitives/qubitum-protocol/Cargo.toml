[package]
name = "qubitum-protocol"
version = "0.1.0"
edition.workspace = true
description = "Core Qubitum protocol primitives for private verifiable AI inference."
publish = false

[lints]
workspace = true

[dependencies]
codec = { workspace = true, features = ["derive"] }
scale-info = { workspace = true, features = ["derive"] }

[features]
default = ["std"]
std = [
	"codec/std",
	"scale-info/std",
]
